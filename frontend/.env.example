# Production API URL Configuration
# Set this in your Vercel Project Environment Variables if needed
# (Defaults to /api/ in production, which Vercel rewrites to the backend index.py function)

VITE_API_URL=/api/
